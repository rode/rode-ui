{
  "name": "rode-ui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "fmt": "prettier --write .",
    "lint": "echo lint",
    "start": "next start",
    "test": "echo test",
    "verify": "prettier --check . && yarn lint && yarn test"
  },
  "dependencies": {
    "next": "10.0.6",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "prettier": "^2.2.1"
  }
}
